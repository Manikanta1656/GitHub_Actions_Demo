name: 01-Buliding-Blocks

on:
    push

jobs:
    echo-world:
        runs-on: ubuntu-latest
        steps:
            - name: Echo Hello World
              run: echo "Hello, World!"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Failed Step
              run: |
                echo "This step will sucess"
                exit 0
            - name: Echo Goodbye
              run: echo "Goodbye, World!"
